{
  "name": "addgame",
  "version": "1.0.0",
  "description": "Adding game (for first graders)",
  "main": "code.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc --watch",
    "server": "npx http-server -p 8888"
  },
  "author": "Alex Cornejo",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^14.1.1",
    "typescript": "^5.2.2"
  }
}
